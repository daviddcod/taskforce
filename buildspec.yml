version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.10 # Updated to your specified version
    commands:
      - echo "Installing dependencies for WDMMORPG2"
      - pip install --upgrade pip
      - pip install -r requirements.txt  # Assumes requirements.txt is in the root
      - pip install django-storages
      - pip install boto3
      - pip install psycopg2

  pre_build:
    commands:
      - echo "Running pre-build commands for WDMMORPG2"
      - python manage.py migrate --settings=WDMMORPG2.settings --noinput  # Updated path
      - python manage.py collectstatic --settings=WDMMORPG2.settings --noinput  # Handle static files

  build:
    commands:
      - echo "Build commands for WDMMORPG2"
      # Insert any additional build commands if needed later

  post_build:
    commands:
      - echo "WDMMORPG2 build and deployment completed."

artifacts:
  files:
    - '**/*'  # Adjust if specific files need to be preserved